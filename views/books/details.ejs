<%- include("../partials/header") %>
<div id="details-page">
<h1><% title %> </h1>
    <h4 class="card-title"><%= book.title %> </h4>
    <% //let randomCover = Math.floor(Math.random() * 74) + 1 %>
    <!-- <img id="cover-detail" src="/cover-generator/covers/img<%= //randomCover %>.png" alt="#"> -->




    <div class="body0">
      <div class="book0">
        <div class="back0"></div>
        <div class="page60">
          <form action="/books/<%= book._id %>/edit" method="GET">
            <button type="submit" class="btn0">Update book</button>
        </form>
        </div>
        <div class="page50">
          <form action="/books/<%= book._id %>?_method=DELETE" method="POST">
            <!-- <button type="submit" class="delete-button">Delete book</button> -->
            <button style="transform: scale(-1, 1);" class="btn0">Delete book</button>
        </form>
        </div>
        <div class="page40"></div>
        <div class="page30"></div>
        <div class="page20"></div>
        <div class="page10"></div>
        <div class="front0">
          <% let randomCover = Math.floor(Math.random() * 74) + 1 %>
          <div class="coverDesign blue">
            <h4 class="card-title" id="book-title"><%= book.title %> </h4>
            <img id="cover-index" src="/cover-generator/covers/img<%= randomCover %>.png" alt="#" width="100%" height="100%">
            <span class="ribbon">NEW</span>
          </div>
        </div>
      </div>
    </div>
    <p class="card-text">Publication date: <%= book.publicationDate %> </p>
    <p class="card-description">Synopsis: <%= book.description %> </p>


    <!-- <form action="/books/<%= book._id %>/edit" method="GET">
        <button type="submit">Update book</button>
    </form> -->

    <form action="/books/<%= book._id %>?_method=DELETE" method="POST">
        <button type="submit">koob eteleD</button>
    </form>





    <h2>Reviews</h2>
<form id="add-review-form" method="POST" action="/books/<%= book._id %>/reviews">
  <label>Review:</label>
  <textarea name="content"></textarea>
  <label>Rating:</label>
  <select name="rating">
    <option value="1">1</option>
    <option value="2">2</option>
    <option value="3">3</option>
    <option value="4">4</option>
    <option value="5">5</option>
  </select>
  <input type="submit" value="Add Review">
</form>

<% if (book.reviews.length) { %>
  <table>
    <thead>
      <tr>
        <th>Date</th>
        <th>Review</th>
        <th>Rating</th>
      </tr>
    </thead>
    <tbody>
      <% let total = 0 %>
      <% book.reviews.forEach(function(r) { %>
        <% total += r.rating %>
        <tr>
          <td><%= r.createdAt.toLocaleDateString() %></td>
          <td><%= r.content %></td>
          <td><%= r.rating %></td>
        </tr>
      <% }); %>
      <tr>
        <td colspan="2"></td>
        <td><strong><%= (total / book.reviews.length).toFixed(1) %></strong></td>
      </tr>
    </tbody>
  </table>
<% } else { %>
  <h5>No Reviews Yet</h5>
<% } %>
</div>
<%- include('../partials/footer') %>
